import React from 'react';
import type { IGameDetails } from '@/types';

interface ICombatStageProps {
  details: IGameDetails;
}

export function CombatStage({ details }: ICombatStageProps): React.ReactElement {
  return <></>;
}
